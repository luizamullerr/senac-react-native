function ola(nome: string): string {
    return `Olá ${nome}`
}

function tchau(nome: string): string {
    return `Até mais ${nome}`
}

export {ola, tchau} //esse modulo vai exportar estas duas funções